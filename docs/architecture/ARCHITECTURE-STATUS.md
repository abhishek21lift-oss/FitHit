# Architecture Status

Contract v0.2 is frozen.

Implementation order: foundation → domain → database/RLS → identity → directory/care teams → consent/authorization → clinical core/private notes → timeline → handoff/referral → documentation AI → contextual AI → security hardening → E2E/production validation.

No quality gate may be weakened to obtain green CI.
